"""MiniStore — a tiny line-based TCP key/value server.

A throwaway Python warm-up for the CodeCrafters "Build Your Own Redis" track.
The point isn't to clone Redis — it's to meet Python's socket, bytes, and
concurrency primitives once, against a dead-simple text protocol, so that the
real RESP parsing in week 3 is the *only* new thing.

See README.md for the milestone checklist.
"""
